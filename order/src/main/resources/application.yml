server:
  port: 8080

spring:
  profiles:
    active: dev
  application:
    name: order-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos

logging:
  config: classpath:logback-spring.xml
